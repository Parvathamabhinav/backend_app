<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/stylesheets/index.css">
    <link rel="icon" type="image/png" href="/icon.png">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script>
        function revealForm() {
            const form = document.getElementById('form');
            const title = document.getElementById('title');

            // Form rises into view
            form.classList.remove('opacity-0', 'translate-y-32');
            form.classList.add('opacity-100', 'translate-y-0');

            // Title moves up slightly (but stays centered overall)
            title.classList.add('mb-10');
        }
    </script>


    <title>Research Project</title>
</head>
<body>
    <div
        id="container"
        class="min-h-screen w-full bg-zinc-900 text-white
                flex flex-col items-center justify-center
                transition-all duration-[1500ms] ease-out"
    >

    <!-- Heading -->
    <h1
        id="title"
        onclick="revealForm()"
        class="cursor-pointer text-5xl font-extrabold tracking-tight text-center
            transition-all duration-[1500ms] ease-out mb-5"
    >
        Welcome to <span class="text-blue-500">Our Project</span>
    </h1>

    <!-- Form -->
   <form action="/create" method="post" class="flex flex-col gap-4 w-80">

    <input
        class="px-5 py-2 rounded-lg border border-zinc-700 bg-transparent"
        type="text"
        name="name"
        placeholder="Enter name"
        required
    >

    <input
        class="px-5 py-2 rounded-lg border border-zinc-700 bg-transparent"
        type="text"
        name="occupation"
        placeholder="Enter occupation"
        required
    >

    <input
        class="px-5 py-2 rounded-lg border border-zinc-700 bg-transparent"
        type="text"
        name="image"
        placeholder="Image URL"
        required
    >

    <button
        class="px-6 py-2 bg-blue-500 rounded-lg self-center"
    >
        Create
    </button>

</form>


    </div>


</body>
</html>