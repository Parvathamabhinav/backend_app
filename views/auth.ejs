<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/stylesheets/index.css">
    <link rel="icon" type="image/png" href="/icon.png">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Research Project</title>
</head>
<body>
    <div class="min-h-screen w-full  bg-zinc-900 p-10 text-white flex  flex-col items-center justify-center">
        <h1 class="text-4xl font-bold mb-6">Scotch</h1>
        <form action="/<%= mode === 'register' ? 'register' : 'login' %>" method="post" class="flex flex-col gap-4 w-80">
            <input class=" px-5 py-2 rounded-lg border-2 border-zinc-800 outline-none bg-transparent" type="text" name="name" placeholder="Enter username">
            <% if (mode === 'register') { %>
                 <input class=" px-5 py-2 rounded-lg border-2 border-zinc-800 outline-none bg-transparent" type="email" name="email" placeholder="Enter email">
            <% } %>
            <input class=" px-5 py-2 rounded-lg border-2 border-zinc-800 outline-none bg-transparent" type="password" name="password" placeholder="Enter password">
            <input class=" px-6 py-2 rounded-lg bg-blue-500 text-white self-center" type="submit" value="<%= mode === 'register' ? 'Register' : 'Login' %>">
        </form>
    </div>
</body>
</html>