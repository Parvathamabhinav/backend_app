<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />

<link rel="icon" type="image/png" href="/icon.png">
<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<script src="https://unpkg.com/lucide@latest"></script>

<title>Scotch • Login</title>

<!-- Fonts -->
<link href="https://api.fontshare.com/v2/css?f[]=satoshi@300,400,500,700,900&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">

<style>
  @keyframes growHeight {
    from { max-height: 70px; opacity: 0; }
    to   { max-height: 520px; opacity: 1; }
  }

  body { font-family: 'Satoshi', sans-serif; }
  .brand-font { font-family: 'Playfair Display', serif; }
</style>
</head>

<body class="min-h-screen flex items-center justify-center
             bg-gradient-to-br from-black via-zinc-900 to-black text-white">

<!-- CARD -->
<div
  class="relative w-[380px]
         bg-white/5 backdrop-blur-2xl
         border border-white/10
         rounded-3xl overflow-hidden
         shadow-[0_0_40px_rgba(59,130,246,0.25)]
         animate-[growHeight_4s_linear_forwards]">

  <!-- Glow -->
  <div class="absolute -inset-2 rounded-3xl
              bg-gradient-to-r from-blue-500/20 via-purple-500/20 to-blue-500/20
              blur-3xl opacity-40 -z-10"></div>

  <!-- CONTENT -->
  <div class="p-8">

    <!-- BRAND -->
    <h1 class="brand-font text-5xl text-center mb-6
               font-semibold tracking-[-0.04em]
               bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400
               bg-clip-text text-transparent
               drop-shadow-[0_10px_30px_rgba(147,51,234,0.35)]">
      Scotch
    </h1>

    <!-- MESSAGE BLOCK (FIXED) -->
    <% if (error || success) { %>
      <div
        class="mb-6 px-5 py-3 rounded-xl text-center text-sm font-medium
               border backdrop-blur-md
               animate-[fadeUp_0.6s_ease-out]
               <%= error
                 ? 'bg-red-500/10 text-red-400 border-red-500/30'
                 : 'bg-green-500/10 text-green-400 border-green-500/30'
               %>">
        <%= error || success %>
      </div>
    <% } %>

    <!-- FORM -->
    <form action="/login" method="post" class="space-y-6">

      <!-- Username -->
      <div class="relative">
        <i data-lucide="user"
           class="absolute left-4 top-1/2 -translate-y-1/2 text-zinc-400"></i>

        <input
          type="text"
          name="name"
          placeholder="Username"
          class="w-full pl-12 pr-4 py-4 rounded-xl
                 bg-black/30 border border-white/10
                 outline-none focus:border-blue-500 transition
                 text-sm tracking-wide
                 placeholder:text-zinc-500"/>
      </div>

      <!-- Password -->
      <div class="relative">
        <i data-lucide="lock"
           class="absolute left-4 top-1/2 -translate-y-1/2 text-zinc-400"></i>

        <input
          type="password"
          name="password"
          placeholder="Password"
          class="w-full pl-12 pr-4 py-4 rounded-xl
                 bg-black/30 border border-white/10
                 outline-none focus:border-blue-500 transition
                 text-sm tracking-wide
                 placeholder:text-zinc-500"/>
      </div>

      <!-- Button -->
      <button
        type="submit"
        class="w-full py-4 rounded-xl font-semibold tracking-wide
               bg-gradient-to-r from-blue-500 to-purple-500
               hover:opacity-90 transition">
        Login
      </button>

    </form>

    <p class="mt-6 text-center text-sm text-zinc-400 tracking-wide">
      Don’t have an account?
      <a href="/" class="text-blue-400 hover:text-blue-300 transition">
        Create one
      </a>
    </p>

  </div>
</div>

<script>
  lucide.createIcons();
</script>

</body>
</html>
